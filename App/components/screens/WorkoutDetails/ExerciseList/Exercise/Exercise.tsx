import React from 'react'
import { StyleSheet, Text } from 'react-native'

interface ExerciseProps {
  exercise: string
}

export default ({ exercise }: ExerciseProps) => (
  <Text>exercise</Text>
)

const styles = StyleSheet.create({
})
